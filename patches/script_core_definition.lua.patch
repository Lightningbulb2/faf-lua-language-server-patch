--- "lua-language-server-3.18.2/script/core/definition.lua"
+++ "faf-lua-language-server-patch/script/core/definition.lua"
@@ -218,6 +218,10 @@
         if src.type == 'doc.param' then
             goto CONTINUE
         end
+        
+        if src.start == -1 and src.finish == -1 then
+            goto CONTINUE
+        end
 
         results[#results+1] = {
             target = src,
